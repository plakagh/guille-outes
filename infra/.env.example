# Used by pnpm scripts — copy to .env.local and fill in PROD_DB_URL

# Local DB (fixed — matches config.toml port)
SUPABASE_DB_URL=postgresql://postgres:postgres@localhost:54422/postgres

# Production DB — used by: pnpm db:push:prod
PROD_DB_URL=postgresql://postgres:CHANGEME@<VPS_IP>:5433/postgres
